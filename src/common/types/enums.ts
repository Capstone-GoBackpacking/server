export enum EStatus {
  enable = 'ENABLE',
  disable = 'DISABLE',
  pending = 'PENDING',
}

export enum EVote {
  up = 'UP',
  down = 'DOWN',
}

export enum EDesign {
  template = 'TEMPLATE',
  build = 'BUILD',
}
